entity: PostMaterial
processor: ./processors/post-material-processor
items:
  post_material{1..100}:
    post: '@post*'
    material: '@material*'
    quantity: '{{datatype.number({ min: 1, max: 10 })}}'
    totalWeight: '{{datatype.float({ min: 0.1, max: 20, precision: 0.01 })}}'